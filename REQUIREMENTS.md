# 要件定義

マルチテナント タスク管理アプリの要件。インターン研修課題として、認証・ユーザー管理・マルチテナンシーの設計を理解したうえで実装することを目的とする。

## 全体像

- 組織(テナント)ごとにタスクとユーザーが分かれる。他組織のデータは一切見えない
- 認証は Firebase Authentication(メールアドレス + パスワード)
- ロールは `admin`(管理者) / `member`(メンバー) の2種類
- フロントエンドは Next.js、データストアは Firestore(NoSQL)

## 1. 組織とユーザーの関係

- **1ユーザーは1組織にのみ所属する**(複数組織への同時所属はサポートしない)
- サインアップの入り口は2種類
  1. **新規組織作成**: サインアップ時に組織名を入力し、新しい組織を作成する。作成した本人が自動的にその組織の `admin` になる
  2. **招待経由での参加**: 招待リンク(トークン付きURL)からアクセスした場合、組織名の入力は不要。メールアドレス・パスワードのみ入力し、招待時に指定されたロールで対象組織の `member`(または `admin`)として参加する
- 既に別組織に所属しているユーザーが、他組織の招待リンクを踏んだ場合は **エラーで弾く**(「既に別の組織に所属しています」)。組織移動機能は今回のスコープ外

## 2. 認証

- Firebase Authentication のメール+パスワード認証を利用
- ログイン、新規登録、ログアウトが一通り動作すること

## 3. ユーザー管理

- ユーザー管理ページが存在する
- **管理者(admin)のみ**が以下を行える
  - ユーザーの招待(招待リンクの発行)
  - 既存メンバーのロール変更(member ⇔ admin)
- **メンバー(member)** はユーザー管理ページを閲覧できるが、招待・ロール変更などの操作はできない
- 招待はメールアドレス宛のリンク方式。実際のメール送信機能(SendGrid等の外部サービス連携)は実装せず、管理者が発行されたURLをコピーして手動で共有する運用とする
- **組織内の admin が自分1人だけの場合、その本人は自分自身を member に降格できない**(組織が誰も管理できない状態になることを防ぐ)

## 4. タスク管理

- タスクの作成・一覧・編集・削除ができる
- タスクの項目
  - タイトル
  - 説明
  - ステータス: 未着手 / 進行中 / 完了
  - 担当者(組織内のユーザーから選択)
  - 期限
- ステータスは未着手/進行中/完了を自由に行き来できる(一方通行の制約は設けない)
- **同じ組織のメンバーであれば誰でも全タスクを編集・削除できる**。担当者フィールドは表示・整理用の情報であり、編集権限には影響しない(担当者以外が編集不可、のような制約は設けない)

## 5. マルチテナンシー(データ分離)

- 組織Aのユーザーは、組織Bのタスク・ユーザー情報に一切アクセスできない(閲覧・書き込みともに不可)
- データ構造・セキュリティルールの両方でテナント分離を保証する

## スコープ外(今回はやらないこと)

- 実際のメール送信(招待メールの自動送信)
- 1ユーザーが複数組織に所属するケース
- 組織間のユーザー移動
- admin / member 以外のロール、カスタム権限
- タスクのコメント、添付ファイル、通知などの付加機能
